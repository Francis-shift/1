# 画像驱动个性化反馈 · 第一版 demo

验证目标:**同一个错误,在不同学习者画像下产生不同反馈** —— 证明"画像"这个
变量真的能改变系统的反馈方式。第一版**不接 VS Code、不接真实数据**,用最小
代价先把"用户特征 → 触发辅助策略 → 给出具体帮助"这条链路跑起来。

## 快速运行

```bash
python3 demo/demo.py           # 3 个错误案例 × 3 份画像 的反馈对照矩阵
python3 demo/demo.py --check   # 先核验错误案例确实会失败,再生成反馈
python3 demo/run_case_check.py # 只核验:参考程序全过、3 个错误案例复现失败
```
纯标准库,无需安装依赖。学生程序在独立子进程中带超时执行,安全。

## 目录结构与分工

```
demo/
├── cases/cases.py          错误案例集 + BRAFAR 式诊断包   【房年朗】✅
├── run_case_check.py       核验案例真会失败的自检脚本      【房年朗】✅
├── demo.py                 主引擎:画像→规则→四旋钮→组装反馈【房年朗搭骨架】✅
├── profiles_and_rules.py   画像 / 6条规则 / 分级话术片段(含示例)【黄新意填充】⬜
├── sample_output.txt       一次完整运行的输出存档(看效果不用跑)
├── make_pipeline_figure.py 生成 BRAFAR 流水线图(文档配图用)
└── README.md               本文件
```

- **房年朗已完成**:3 个真实错误案例(取自 Refactory,已实测复现失败)、
  BRAFAR 诊断过程整理(见 `docs/BRAFAR技术诊断过程整理.md`)、demo 骨架与
  串联逻辑、自检脚本。
- **等黄新意填充**:`profiles_and_rules.py` 里的 3 份画像、6 条规则、3 档反馈
  模板。房年朗已放入一份**可跑通的示例填充**,黄新意在原处修改即可,改完
  直接 `python3 demo/demo.py` 看效果,不用动其它文件。

## 设计要点(为什么这样拆)

1. **诊断层 vs 画像层分离**:错误"改什么"由诊断包客观给出(对所有人相同);
   "怎么对这个人讲"由画像层决定(因人而异)。这正是本项目相对 BRAFAR 的增量。
2. **画像 → 风格的四个旋钮**:定位深度(引导强度)/ 解释深度 / 代码供给 / 语气。
   6 条规则把画像特征(K/D/H/E)映射到旋钮;反馈由旋钮**组装**而成——
   规则改哪个旋钮,输出就变哪部分,差异可解释、可追溯。
3. **定位深度 = BRAFAR"由粗到细"的分级截取**:新手只给粗区域(fault_region),
   进阶给行号(fault_line),熟练给行号+最小改法——定位深度本身就是个性化刻度。
4. **可替换插槽**:黄新意的部分全部集中在一个文件、以数据+片段形式暴露,
   方便迭代;房年朗的骨架不需要因画像调整而改动。
5. **结论靠机器校验**:demo 结尾自动断言"每个案例的 3 份反馈两两不同",
   不靠肉眼判断,退出码非 0 即失败。

## 已知边界(留给下一版)

- 诊断包目前是**预先整理**的(第一版);正式版把 `demo.py: make_diag()` 换成
  实时调用 BRAFAR 工具产出 fault_line / patched。
- 画像是**手工模拟**的;后续替换为从真实行为数据(Refactory/CS1QA 等)算出的画像。
- 反馈用**模板**生成;后续可把诊断包 + 画像 + 风格拼进提示词,交 LLM 生成更自然的话术。

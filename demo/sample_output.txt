========================================================================
错误案例:case1_boundary  【边界条件错误(逻辑错,答案错误)】
来源:Refactory question_1 / wrong_1_001.py
错误代码:
    1| def search(x, seq):
    2|     for i, e in enumerate(seq):
    3|         if x < e:  <-- 出错行
    4|             return i
    5|     return len(seq)

  ┌─ 画像【新手型 · 小 A】(K=low D=low E=low H=high_dependence)
  │  命中规则:R1_K低_补概念, R2_D低_给位置与改法, R3_H高依赖_封顶不给完整补丁
  │  风格旋钮:定位深度=medium 解释=concept 代码=snippet 语气=gentle
  │  ── 系统反馈 ──
  │   别急,这个错误很多同学都会遇到,我们一步步来 🙂
  │   问题在第 3 行,类型是边界条件错误(逻辑错,答案错误)。
  │   相关概念:「严格小于 <」和「小于等于 <=」在边界(相等)时行为不同,查找类问题要特别小心 = 这个临界点。
  │   修改方向:`<` 改为 `<=`(先不给完整代码)
  │   按这个方向自己改改看,并想想为什么这样能覆盖漏掉的情况。
  └────────────────────────────────────────────────────────────
  ┌─ 画像【进阶型 · 小 B】(K=mid D=mid E=mid H=balanced)
  │  命中规则:R4_D中_行号加原因
  │  风格旋钮:定位深度=medium 解释=step 代码=none 语气=neutral
  │  ── 系统反馈 ──
  │   运行没有全部通过,来看一下问题。
  │   问题在第 3 行,类型是边界条件错误(逻辑错,答案错误)。
  │   原因:比较用了严格小于 `x < e`,漏掉了 x 等于当前元素的情况;题目要求 x 等于某元素时也应停在该位置,应为 `x <= e`。
  │   按这个方向自己改改看,并想想为什么这样能覆盖漏掉的情况。
  └────────────────────────────────────────────────────────────
  ┌─ 画像【熟练型 · 小 C】(K=high D=high E=high H=independent)
  │  命中规则:R5_D高_只给方向, R6_E高_从简
  │  风格旋钮:定位深度=strong 解释=minimal 代码=none 语气=terse
  │  ── 系统反馈 ──
  │   问题出在「循环里的比较判断」附近,类型是边界条件错误(逻辑错,答案错误)。先自己找找具体是哪一行。
  │   想一想:这一行在什么输入下会得到不符合预期的结果?自己改一版,跑一遍测试再看。
  └────────────────────────────────────────────────────────────
  [校验] 本案例 3 份反馈两两不同:✓

========================================================================
错误案例:case2_unbound  【变量未定义 / 空序列边界(运行时错误 UnboundLocalError)】
来源:Refactory question_1 / wrong_1_002.py
错误代码:
    1| def search(x, seq):
    2|     for i in range(len(seq)):
    3|         if x <= seq[i]:
    4|             return i
    5|     return i + 1  <-- 出错行

  ┌─ 画像【新手型 · 小 A】(K=low D=low E=low H=high_dependence)
  │  命中规则:R1_K低_补概念, R2_D低_给位置与改法, R3_H高依赖_封顶不给完整补丁
  │  风格旋钮:定位深度=medium 解释=concept 代码=snippet 语气=gentle
  │  ── 系统反馈 ──
  │   别急,这个错误很多同学都会遇到,我们一步步来 🙂
  │   问题在第 5 行,类型是变量未定义 / 空序列边界(运行时错误 UnboundLocalError)。
  │   相关概念:循环里定义的变量,在循环一次都没执行时是「不存在」的;依赖循环变量做兜底返回是危险的,应改用与循环无关的量。
  │   修改方向:`return i + 1` 改为 `return len(seq)`(先不给完整代码)
  │   按这个方向自己改改看,并想想为什么这样能覆盖漏掉的情况。
  └────────────────────────────────────────────────────────────
  ┌─ 画像【进阶型 · 小 B】(K=mid D=mid E=mid H=balanced)
  │  命中规则:R4_D中_行号加原因
  │  风格旋钮:定位深度=medium 解释=step 代码=none 语气=neutral
  │  ── 系统反馈 ──
  │   运行没有全部通过,来看一下问题。
  │   问题在第 5 行,类型是变量未定义 / 空序列边界(运行时错误 UnboundLocalError)。
  │   原因:循环没命中时用 `return i + 1`,但当 seq 为空时循环体从未执行,变量 i 从未被赋值,触发 UnboundLocalError;且非空时正确的兜底应是 `len(seq)` 而不是 `i + 1`。
  │   按这个方向自己改改看,并想想为什么这样能覆盖漏掉的情况。
  └────────────────────────────────────────────────────────────
  ┌─ 画像【熟练型 · 小 C】(K=high D=high E=high H=independent)
  │  命中规则:R5_D高_只给方向, R6_E高_从简
  │  风格旋钮:定位深度=strong 解释=minimal 代码=none 语气=terse
  │  ── 系统反馈 ──
  │   问题出在「循环结束之后的返回语句」附近,类型是变量未定义 / 空序列边界(运行时错误 UnboundLocalError)。先自己找找具体是哪一行。
  │   想一想:这一行在什么输入下会得到不符合预期的结果?自己改一版,跑一遍测试再看。
  └────────────────────────────────────────────────────────────
  [校验] 本案例 3 份反馈两两不同:✓

========================================================================
错误案例:case3_indexerror  【空序列下标越界(运行时错误 IndexError)】
来源:Refactory question_1 / wrong_1_004.py
错误代码:
    1| def search(x, seq):
    2|     if x < seq[0]:  <-- 出错行
    3|         return 0
    4|     elif x > seq[-1]:
    5|         return len(seq)
    6|     else:
    7|         seq_enum = [i for i in enumerate(seq)]
    8|         for j in range(len(seq_enum) - 1):
    9|             if x >= seq_enum[j][1] and x <= seq_enum[j + 1][1]:
    10|                 return j + 1

  ┌─ 画像【新手型 · 小 A】(K=low D=low E=low H=high_dependence)
  │  命中规则:R1_K低_补概念, R2_D低_给位置与改法, R3_H高依赖_封顶不给完整补丁
  │  风格旋钮:定位深度=medium 解释=concept 代码=snippet 语气=gentle
  │  ── 系统反馈 ──
  │   别急,这个错误很多同学都会遇到,我们一步步来 🙂
  │   问题在第 2 行,类型是空序列下标越界(运行时错误 IndexError)。
  │   相关概念:用下标或 seq[0]/seq[-1] 访问序列前,必须先确认它非空;空序列是最常见、最容易被忽略的边界。
  │   修改方向:进入下标访问前先判空:`if not seq: return 0`(先不给完整代码)
  │   按这个方向自己改改看,并想想为什么这样能覆盖漏掉的情况。
  └────────────────────────────────────────────────────────────
  ┌─ 画像【进阶型 · 小 B】(K=mid D=mid E=mid H=balanced)
  │  命中规则:R4_D中_行号加原因
  │  风格旋钮:定位深度=medium 解释=step 代码=none 语气=neutral
  │  ── 系统反馈 ──
  │   运行没有全部通过,来看一下问题。
  │   问题在第 2 行,类型是空序列下标越界(运行时错误 IndexError)。
  │   原因:一上来就访问 `seq[0]` 和 `seq[-1]`,没有先处理空序列,空序列时立即 IndexError;整体思路(分三段判断)也比参考实现复杂、易漏边界,BRAFAR 会先做结构对齐再定位。
  │   按这个方向自己改改看,并想想为什么这样能覆盖漏掉的情况。
  └────────────────────────────────────────────────────────────
  ┌─ 画像【熟练型 · 小 C】(K=high D=high E=high H=independent)
  │  命中规则:R5_D高_只给方向, R6_E高_从简
  │  风格旋钮:定位深度=strong 解释=minimal 代码=none 语气=terse
  │  ── 系统反馈 ──
  │   问题出在「函数开头对序列的下标访问」附近,类型是空序列下标越界(运行时错误 IndexError)。先自己找找具体是哪一行。
  │   想一想:这一行在什么输入下会得到不符合预期的结果?自己改一版,跑一遍测试再看。
  └────────────────────────────────────────────────────────────
  [校验] 本案例 3 份反馈两两不同:✓

========================================================================
结论(机器校验通过):同一错误,3 份画像 → 3 份两两不同的反馈,
且差异可解释(每处差异都能追溯到命中的规则与旋钮)。第一版验证达成。
下一步:接入 BRAFAR 实时诊断、真实行为数据与 VS Code。
